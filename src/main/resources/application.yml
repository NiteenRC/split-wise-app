spring:
  application:
    name: SPLIT-WISE-APP
  profiles:
    active:
      - ${SPRING_PROFILES_ACTIVE:local}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always